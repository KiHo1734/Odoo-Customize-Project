<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <template id="employees_skills_template">
    <style>
      .page { padding-top: 0 !important; }
    </style>

    <t t-set="o" t-value="docs[0]"/>

    <!-- เรียก layout -->
    <t t-call="web.external_layout">
      <main class="page">
        <h2 class="text-center">Employee Skills by Department</h2>

        <p>
          รายงานนี้สรุปทักษะของพนักงานแต่ละคน แบ่งตามแผนก ประเภท และระดับความสามารถ
          (พิมพ์เมื่อ: <t t-esc="datetime.datetime.today().strftime('%d/%m/%Y')"/>)
        </p>

        <!-- ดึง department ที่มีพนักงานใน docs -->
        <t t-set="departments" t-value="docs.mapped('employee_id.department_id').filtered(lambda d: d).sorted(key=lambda d: d.name)"/>
        
        <t t-foreach="departments" t-as="department">
          <h3 class="mt16"><t t-esc="department.name"/></h3>

          <table class="table table-sm table-hover o_main_table">
            <thead>
              <tr>
                <th>Employee</th>
                <th>Skill Type</th>
                <th>Skill</th>
                <th>Level</th>
              </tr>
            </thead>
            <tbody>
              <!-- ดึงพนักงานใน department นี้ -->
              <t t-set="dept_employees" t-value="docs.mapped('employee_id').filtered(lambda e: e.department_id.id == department.id).sorted(key=lambda e: e.name)"/>
              
              <t t-foreach="dept_employees" t-as="employee">
                <t t-set="employee_skills" t-value="docs.filtered(lambda s: s.employee_id.id == employee.id)"/>
                <t t-set="first_row" t-value="True"/>
                <t t-foreach="employee_skills" t-as="skill">
                  <tr>
                    <td>
                      <t t-if="first_row">
                        <t t-esc="employee.name"/>
                        <t t-set="first_row" t-value="False"/>
                      </t>
                    </td>
                    <td><t t-esc="skill.skill_type_id.name or ''"/></td>
                    <td><t t-esc="skill.skill_id.name or ''"/></td>
                    <td><t t-esc="skill.skill_level_id.name or ''"/></td>
                  </tr>
                </t>
              </t>
            </tbody>
          </table>
        </t>
      </main>
    </t>
  </template>
</odoo>
